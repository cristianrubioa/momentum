{
  "name": "momentum-card",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/card/momentum-card.ts --bundle --outfile=custom_components/momentum/momentum-card.js --format=esm --minify",
    "dev": "esbuild src/card/momentum-card.ts --bundle --outfile=custom_components/momentum/momentum-card.js --format=esm --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
